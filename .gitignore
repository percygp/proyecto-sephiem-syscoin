# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Convex local backend state (contiene admin key local) — VAL-45
.convex/

# Staging/production env files (solo .example se versiona)
.env.staging
.env.production

# Discord bot secrets — nunca versionar
discord/.env
discord/.env.local
discord/.env.*

# Secretos locales (mnemonics/seeds HD — NUNCA versionar)
.secrets/

# Scratch
/tmp/

# Sub-proyecto de referencia Sephiem-Syscoin: ignorar binarios pesados y deps.
# El código (contracts/, frontend/, docs) sí se versiona; la capa on-chain
# integrada en la app principal vive en src/web3/.
proyecto-sephiem-syscoin/**/node_modules
proyecto-sephiem-syscoin/*.pdf
proyecto-sephiem-syscoin/contracts/artifacts/
proyecto-sephiem-syscoin/contracts/cache/
